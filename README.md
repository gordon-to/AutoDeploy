# AutoDeploy
Use Sinatra and bash to deploy a site from a git repository

This is a set of scripts to listen to github webhooks and clone and copy a repository to a live folder. The script is currently configured to push the Affektive repository. 
